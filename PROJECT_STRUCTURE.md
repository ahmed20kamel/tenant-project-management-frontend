# هيكل المشروع (Project Structure)

هذا المشروع يستخدم **Feature-Based Architecture** لتنظيم الكود بشكل احترافي ومنسق.

## 📁 الهيكل العام

```
frontend/src/
├── app/                    # إعدادات التطبيق
├── assets/                 # الملفات الثابتة (صور، أيقونات)
├── components/             # المكونات المشتركة
│   ├── common/            # مكونات عامة (Button, Card, Dialog)
│   ├── fields/            # حقول النماذج
│   ├── file-upload/       # مكونات رفع الملفات
│   ├── forms/             # مكونات النماذج
│   ├── layout/            # مكونات التخطيط (Navbar, Sidebar, Layout)
│   └── ui/                # مكونات واجهة المستخدم (Chips, LanguageSwitcher)
├── config/                 # ملفات الإعدادات (API, i18n)
├── contexts/              # React Contexts (AuthContext)
├── features/               # Features منظمة حسب الوظيفة
│   ├── admin/             # صفحات ومكونات Super Admin
│   │   ├── pages/
│   │   └── components/
│   ├── auth/              # صفحات المصادقة والتسجيل
│   │   ├── pages/
│   │   └── components/
│   ├── company/           # صفحات إدارة الشركة
│   │   ├── pages/
│   │   └── components/
│   ├── consultants/       # صفحات الاستشاريين
│   │   ├── pages/
│   │   └── components/
│   ├── owners/            # صفحات الملاك
│   │   ├── pages/
│   │   └── components/
│   ├── payments/          # صفحات المدفوعات
│   │   ├── pages/
│   │   └── components/
│   ├── profile/           # صفحات الملف الشخصي
│   │   └── pages/
│   └── projects/          # صفحات ومكونات المشاريع
│       ├── pages/
│       ├── components/
│       ├── view/          # صفحات عرض المشاريع
│       └── wizard/         # معالج إنشاء المشاريع
├── hooks/                 # Custom Hooks
├── pages/                  # الصفحات الرئيسية
│   ├── public/            # الصفحات العامة (Landing, Pricing)
│   ├── HomePage.jsx       # الصفحة الرئيسية للشركة
│   └── DashboardPage.jsx  # صفحة Dashboard
├── services/              # خدمات API
├── styles/                # ملفات CSS
│   ├── components.css
│   ├── design-system.css
│   └── pages.css
└── utils/                 # أدوات مساعدة
    ├── errors/
    ├── formatters/
    └── helpers/
```

## 🎯 مبادئ التنظيم

### 1. Feature-Based Architecture
كل feature له مجلد خاص يحتوي على:
- `pages/` - صفحات الـ feature
- `components/` - مكونات خاصة بالـ feature
- `hooks/` - hooks خاصة بالـ feature (اختياري)
- `services/` - خدمات API خاصة بالـ feature (اختياري)

### 2. Shared Components
المكونات المشتركة بين features توضع في `components/`:
- `common/` - مكونات عامة (Button, Card, Dialog)
- `layout/` - مكونات التخطيط (Navbar, Sidebar, Layout)
- `forms/` - مكونات النماذج
- `ui/` - مكونات واجهة المستخدم

### 3. Public vs Protected Pages
- `pages/public/` - صفحات عامة لا تحتاج مصادقة
- `features/*/pages/` - صفحات محمية تحتاج مصادقة

## 📦 Features

### 🔐 Admin Feature
**المسار:** `features/admin/`
- `AdminLoginPage` - تسجيل دخول Super Admin
- `AdminDashboardPage` - لوحة تحكم Super Admin
- `AdminTenantsPage` - إدارة الشركات
- `AdminCreateCompanyPage` - إنشاء شركة جديدة

### 🔑 Auth Feature
**المسار:** `features/auth/`
- `CompanyLoginPage` - تسجيل دخول الشركة
- `CompanyRegistrationPage` - تسجيل شركة جديدة
- `OnboardingWizardPage` - معالج الإعداد الأولي

### 🏢 Company Feature
**المسار:** `features/company/`
- `CompanySettingsPage` - إعدادات الشركة
- `CompanyUsersPage` - إدارة مستخدمي الشركة

### 📊 Projects Feature
**المسار:** `features/projects/`
- `ProjectsPage` - قائمة المشاريع
- `ProjectView` - عرض المشروع
- `wizard/` - معالج إنشاء/تعديل المشروع
- `view/` - صفحات عرض مراحل المشروع

### 💰 Payments Feature
**المسار:** `features/payments/`
- `PaymentsPage` - قائمة المدفوعات

### 👥 Owners Feature
**المسار:** `features/owners/`
- `OwnersPage` - قائمة الملاك
- `OwnerDetailPage` - تفاصيل المالك

### 👨‍💼 Consultants Feature
**المسار:** `features/consultants/`
- `ConsultantsPage` - قائمة الاستشاريين
- `ConsultantDetailPage` - تفاصيل الاستشاري

### 👤 Profile Feature
**المسار:** `features/profile/`
- `ProfilePage` - الملف الشخصي

## 🔄 كيفية إضافة Feature جديد

1. إنشاء مجلد جديد في `features/`
2. إنشاء `pages/` و `components/` داخل الـ feature
3. إضافة `index.js` لتصدير الصفحات
4. تحديث `App.jsx` بإضافة المسارات الجديدة

## 📝 ملاحظات

- كل feature مستقل ويمكن تطويره بشكل منفصل
- المكونات المشتركة توضع في `components/`
- الصفحات العامة توضع في `pages/public/`
- استخدام `index.js` لتسهيل الـ imports

## 🚀 Backend Structure

```
backend/
├── authentication/        # تطبيق المصادقة والمستخدمين
│   ├── models.py          # نماذج المستخدمين والصلاحيات
│   ├── views.py           # واجهات API
│   ├── serializers.py     # Serializers
│   └── urls.py            # مسارات API
├── projects/              # تطبيق المشاريع
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
└── backend/               # إعدادات Django
    ├── settings.py
    └── urls.py
```

---

**آخر تحديث:** ديسمبر 2025

